services:
  wiremock:
    image: wiremock/wiremock:3.10.0
    container_name: hermetic-wiremock
    restart: unless-stopped
    ports:
      - "${HERMETIC_PORT:-8090}:8080"
    volumes:
      # :z is SELinux relabel-shared (required on Fedora/RHEL). Harmless on other distros.
      - ./fixtures/mappings:/home/wiremock/mappings:ro,z
      - ./fixtures/__files:/home/wiremock/__files:ro,z
    command:
      - --global-response-templating
      - --verbose
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://localhost:8080/__admin/health || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 5s
