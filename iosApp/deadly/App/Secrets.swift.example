// Template — copy to Secrets.swift and fill in tokens for local development.
// CI generates Secrets.swift from GitHub Secrets; this file is checked in as a
// placeholder so the build doesn't fail when Secrets.swift doesn't exist locally.
enum Secrets {
    static let geniusAccessToken = ""
}
