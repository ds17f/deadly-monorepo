// Single source of truth for the community subreddit link (decision I).
// Rendered in the notifications inbox footer (and anywhere else that points
// people at community news). Keep this the only place the handle is hardcoded.
export const SUBREDDIT_HANDLE = "r/thedeadlyapp";
export const SUBREDDIT_URL = "https://www.reddit.com/r/thedeadlyapp";
